const root=document.querySelector("#setup");
root.innerHTML=`<div class="shell"><header class="brand"><strong>Barbearia App</strong><span class="badge">IMPLANTAÇÃO</span></header><section class="hero"><h1>Publique sua barbearia.</h1><p>Crie a identidade e o acesso do proprietário. Profissionais, serviços e ofertas são configurados no painel.</p></section><div id="message"></div><form id="setup-form" class="form"><label>Chave de implantação<input name="setupKey" type="password" required></label><label>Nome da barbearia<input name="name" required maxlength="80"></label><label>Identificador do link<input name="tenant" pattern="[a-z0-9-]{3,50}" placeholder="barbearia-do-bruno" required></label><label>Cor principal<input name="primaryColor" type="color" value="#D3A84C"></label><label>Endereço<input name="address"></label><label>WhatsApp<input name="whatsapp" inputmode="tel"></label><label>E-mail do proprietário<input name="email" type="email" required></label><label>Senha do proprietário<input name="password" type="password" minlength="10" required></label><button class="cta">Criar barbearia</button></form></div>`;
document.querySelector("#setup-form").onsubmit=async(event)=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.currentTarget));const message=document.querySelector("#message");try{const response=await fetch("/api/setup/bootstrap",{method:"POST",headers:{"content-type":"application/json","x-setup-key":data.setupKey},body:JSON.stringify({tenant:data.tenant,name:data.name,primaryColor:data.primaryColor,address:data.address,whatsapp:data.whatsapp,owner:{email:data.email,password:data.password}})});const result=await response.json();if(!response.ok)throw new Error(result.error?.message||"Falha na configuração.");message.innerHTML=`<div class="success-inline">Barbearia criada. <a href="./admin.html">Entrar no painel</a></div>`;event.currentTarget.remove()}catch(error){message.innerHTML=`<div class="alert">${error.message}</div>`}};
